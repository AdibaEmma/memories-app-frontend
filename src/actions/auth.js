export const signin = () => async dispatch => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}

export const signup = () => async dispatch => {
    try {
        
    } catch (error) {
        
    }
}